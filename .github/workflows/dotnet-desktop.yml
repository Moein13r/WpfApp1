name: CI/CD - Build and Deploy WPF App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Setup .NET
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.x'

      # Step 3: Restore dependencies
      - name: Restore dependencies
        run: dotnet restore

      # Step 4: Build the WPF project
      - name: Build solution
        run: dotnet build --configuration Release --no-restore

      # Step 5: Publish the app
      - name: Publish the app
        run: dotnet publish --configuration Release --output ./publish

      # Step 6: Deploy to server
      - name: Deploy to Server
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            # Create deployment directory if it doesn't exist
            mkdir -p /var/www/wpf-app
            # Copy files to the server
            scp -P ${{ secrets.SSH_PORT }} -r ./publish/* ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/var/www/wpf-app
            # (Optional) Restart any related services, e.g., Windows service
